# Cổng 1 — Override with environment variables (see README)
coordinator.base.url=http://localhost:8080
gate.id=1
process.seconds=40
db.url=jdbc:mysql://localhost:3306/gate_parking?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=
